package feature;

import javax.swing.*;

public class BienDoi extends JPanel {
    enum Type {Them, ThayDoi, Chuyen};
    
    public BienDoi(Type type){
        if(type == Type.Them) {
            
        } 
    }
}
